import React from 'react';

const Events = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Events</h1>
      <p>List of events will appear here once the logic is wired up.</p>
    </div>
  );
};

export default Events;
